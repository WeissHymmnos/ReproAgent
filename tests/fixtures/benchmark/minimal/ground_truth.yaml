report_id: "minimal"
broker: "Mock"
report_date: "2023-01-01"
report_title: "Minimal Test Report"
backtest_params:
  start_date: "2023-01-02"
  end_date: "2023-02-10"
factors:
  - name: "mock_momentum"
    name_cn: "模拟动量因子"
    formula: "close / Ref(close, 5) - 1"
    formula_latex: ""
    input_fields: ["close"]
    rebalance_frequency: "monthly"
    universe: "全A股"
    lookback_window: 5
    parameters:
      momentum_period: 5
    reported_metrics:
      # Aligned to local fixture backtest after data-guards keep session-1 bars.
      ic_mean: -0.3333333333333333
      ic_ir: -0.3461093276215864
      # Mock report never states LS / Sharpe / MDD; 0.0 placeholders were
      # treated as claimed targets and made `benchmark --run minimal` fail.
      long_short_return: null
      sharpe_ratio: null
      max_drawdown: null
    annotation_notes: "Mock factor；IC/ICIR 对齐 synthetic prices fixture；未标注收益类指标"
